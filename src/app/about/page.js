import React from 'react';

const About = () => {
    // throw new Error('error')
    return (
        <div>
            <h1>This Is About Page</h1>
        </div>
    );
};

export default About;